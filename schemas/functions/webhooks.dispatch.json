{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "webhooks.dispatch",
  "description": "Hand one fact to the reaction layer from outside this deployment's Action bus. Idempotent on event_id.",
  "type": "object",
  "required": ["event_type"],
  "properties": {
    "event_type": {"type": "string"},
    "event_id": {"type": "string"},
    "service": {"type": "string"},
    "payload": {"type": "object"}
  },
  "additionalProperties": false
}
